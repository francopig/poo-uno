package claseinterfaces;

public interface Reseteable {
	//reset() (y cualquier otro método)
	//por defecto es público y abstracto entonces no hace falta
	//escribir public y abstract
	
	void reset();
}
